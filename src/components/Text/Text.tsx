import React from 'react';
import { Text as NativeText, TextProps, ViewProps } from 'react-native';

type Props = {} & TextProps;

export const Text = ({  }: Props) => {
  return <NativeText>asd</NativeText>;
};
