type IColors = {
  // [key in keyof typeof Colors]: string;
  primary: string;
  border: string;
  white: string;
  black: string;
};

export const Colors: IColors = {
  primary: '#3FD9FB',
  border: '#C4C4C4',

  white: '#ffffff',
  black: '#000000',
};
